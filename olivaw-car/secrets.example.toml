# Copy to `secrets.toml` (gitignored) and fill in. Read by build.rs.
WIFI_SSID = "MyHomeWifi"
WIFI_PASS = "correct horse battery staple"
# IPv4 address or hostname of the olivaw-hub broker (LAN IP of the Mac, a VPS, or a Tailscale IP).
MQTT_HOST = "192.168.1.50"
MQTT_PORT = "1883"
# Leave empty for the embedded broker's default anonymous access.
MQTT_USER = ""
MQTT_PASS = ""
